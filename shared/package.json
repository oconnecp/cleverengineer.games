{
  "name": "dragon-shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf ../frontend/shared && rimraf ../backend/shared && cpy ./src/**/* ../frontend/shared/ --flat && cpy ./src/**/* ../backend/shared/ --flat"
  },
  "devDependencies": {
    "cpy-cli": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "dependencies": {
    "rimraf": "^6.0.1"
  }
}
